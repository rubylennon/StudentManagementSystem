<!--if user is signed in show the edit user link-->
<% if user_signed_in? %>
  <li>
    <%= link_to('Edit registration', edit_user_registration_path) %>
  </li>
<!--else show the register link-->
<% else %>
  <li>
    <%= link_to('Register', new_user_registration_path) %>
  </li>
<% end %>